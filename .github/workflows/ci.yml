name: Node.js CI

on:
<<<<<<< HEAD
=======
  push:
    branches: ["*"]
>>>>>>> main
  pull_request:
    branches: [main]

jobs:
  ci-checks:
    runs-on: ubuntu-latest

    steps:
<<<<<<< HEAD
      # Checkout repository code
      - name: Checkout Code
        uses: actions/checkout@v4
=======
      # Checkout repository
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
>>>>>>> main

      # Setup Node.js
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # Install dependencies
      - name: Install Dependencies
        run: npm ci

<<<<<<< HEAD
      # Run lint check
=======
      # Run ESLint
>>>>>>> main
      - name: Run Lint
        run: npm run lint

      # Run test cases
      - name: Run Tests
        run: npm test

      # Security vulnerability scan
      - name: Security Scan
        run: npm audit --audit-level=high

<<<<<<< HEAD
      # SonarCloud scan
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        with:
          projectKey: devops-traveller_login-api-ci-project
          organization: devops-traveller
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
=======
      # SonarCloud analysis
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: >
            -Dsonar.organization=mpusunuri
            -Dsonar.projectKey=mpusunuri_login-api-ci-project
            -Dsonar.projectName=login-api-ci-project
>>>>>>> main
